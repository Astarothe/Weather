export const weatherAll = (state) => state.weather;

export const weatherStats = (state) => state.weather.weather;

export const weatherConditions = (state) => state.weather.conditions;

export const weatherForecast = (state) => state.weather.forecast;

export const weatherCity = (state) => state.weather.city;

export const loading = (state) => state.loading;

export const loadingLoad = (state) => state.loading.loading;
